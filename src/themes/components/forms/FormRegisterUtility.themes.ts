import { StyleSheet } from "react-native";
import { darkBlack, lightGray } from "../../_varibles";

export const styles = StyleSheet.create({
  container: {
    marginHorizontal:15,
    margin: 12,
    padding: 10,
    backgroundColor: lightGray,
    borderRadius: 16,
    borderColor: darkBlack,
    width: '80%',
    justifyContent:"center",
    alignSelf:"center",
  },
});
